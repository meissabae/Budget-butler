// Central place for the backend URL. Change this if your Spring Boot app
// runs on a different port or address.
export const environment = {
  production : true,
  apiUrl: 'https://budget-butler-backend.onrender.com/api'
};
